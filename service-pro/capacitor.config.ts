import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kz.servicepro.app',
  appName: 'ServicePro.kz',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
